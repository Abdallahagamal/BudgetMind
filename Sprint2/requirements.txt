# Sprint 2 — Task Classifier
# Embeddings are consumed from Sprint 1 artifacts, so sentence-transformers
# is not required here (see Sprint1/budgetmind_embeddings/requirements.txt).
numpy>=1.24
# Pinned (not just floored) to the version the finalized .pkl models were
# actually trained/pickled with, to avoid InconsistentVersionWarning /
# silent behavior drift when a newer scikit-learn is installed.
scikit-learn==1.7.2
joblib>=1.3

redis>=5.0

# Dev / test only
pytest>=8.0
fakeredis>=2.20
